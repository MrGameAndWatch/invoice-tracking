language: python
python:
  - 3.8

sudo: required

services:
  - docker

install:
  - cd invoice-service/
  - pip install -r requirements.txt

jobs:
  include:
    - stage: tests
    name: "Unit Tests"
    script:
      - python -m unittest discover -s "." -p "*_test.py"
