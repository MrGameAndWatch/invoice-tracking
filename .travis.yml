- matrix:
  include:
    - language: python
      python:
        - 3.8
      before_script:
        - cd invoice-service/
        - pip install -r requirements.txt
      script:
        - python -m unittest discover -s "." -p "*_test.py"
    - language: node.js
      node_js: "12.2.0"
      before_script:
        - cd employee-fronted/
        - yarn install
        - yarn test:unit
